import type { Meta, StoryObj } from "@storybook/react";
import CheckFullIcon from "./check-full";

const meta = {
  title: "Icons/CheckFull",
  component: CheckFullIcon,
  parameters: {
    layout: "centered",
  },
  argTypes: {
    size: {
      control: {
        type: "number",
      },
    },
    color: {
      control: {
        type: "color",
      },
    },
  },
} satisfies Meta<typeof CheckFullIcon>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Component: Story = {
  args: {
    color: "#222222",
    size: 24,
  },
};
